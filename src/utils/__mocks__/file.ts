import { Effect } from 'effect'

export const checkDir = (dir: string) => Effect.succeed(dir)
export const makeDir = (dir: string) => Effect.succeed(dir)
export const write = (file: string) => Effect.succeed(file)
